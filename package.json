{
  "name": "media-recorder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode development",
    "build": "tsc && vite build --mode production --base /media-recorder",
    "build:staging": "tsc && vite build --mode staging",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.57.1",
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.11.0",
    "@ffmpeg/ffmpeg": "^0.11.6",
    "rxjs": "^7.8.0"
  }
}
